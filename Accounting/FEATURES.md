# 機能一覧 — Accounting(会計経理支援システム)

モノレポ全体の機能はルート [../FEATURES.md](../FEATURES.md)。本表は Accounting 固有。
状態: **実装済**(実装+テスト) / **雛形**([実装予定]の stub) / **仕様**(定義のみ)。

| ID | 機能 | 状態 | 実装/定義 | 関連要件 |
|---|---|---|---|---|
| FEAT-AC01 | プロジェクト足場(CLAUDE.md ルーター・docs 正本・管理表・workspace・config) | 実装済 | [CLAUDE.md](CLAUDE.md), [docs/](docs/) | REQ-AC01, REQ-AC03 |
| FEAT-AC02 | 運用 CLI(`python -m scripts.cli`: test / hooks install / sync / mirror) | 実装済 | [scripts/cli.py](scripts/cli.py) | REQ-AC01 |
| FEAT-AC03 | 削除可能性テスト(core が Magi/TradeCouncil 非依存)+ docs lint(適用開始日) | 実装済 | [tests/](tests/) | REQ-AC02, REQ-AC03 |
| FEAT-AC04 | Claude 安全フック(危険操作ブロック・正本/秘密の保護・編集後テスト) | 実装済 | [scripts/hooks/](scripts/hooks/) | REQ-EX05 |
| FEAT-AC05 | 月次レビュー シナリオ(LLM 召喚は `../shared` ブリッジ経由) | 雛形 | [scenarios/monthly-review.md](scenarios/monthly-review.md) | REQ-EX02 |
| FEAT-AC06 | MoneyForward API 疎通スパイク(設定を使い OAuth → offices。手動実行・実 creds 必要。token リクエスト組立・offices 件数抽出は純粋関数として単体テスト済) | **会計・経費とも実 API 疎通 確認済(2026-06-14。会計=OAuth→`/v2/tenant`、経費=OAuth→`/api/external/v1/offices`)** | [scripts/spike_moneyforward.py](scripts/spike_moneyforward.py) | REQ-EX01, REQ-EX06 |
| FEAT-AC10 | MoneyForward API 設定の仕組み(**会計/経費の2系統**・非秘密 config + 秘密 env の解決・`ac mf config [--product]` 表示/検証・`ac mf authorize` で認可 URL 生成→ブラウザ起動) | 実装済 | [core/moneyforward.py](core/moneyforward.py), [core/config.py](core/config.py), [config/moneyforward.config.json](config/moneyforward.config.json) | REQ-EX06 |
| FEAT-AC11 | OAuth ログイン補助(`ac mf login`/`refresh`/`token`。**会計=loopback リスナで `code` 自動受信**、**経費=OOB で `code` を貼り付け**(`--code` で非対話)。access/refresh を gitignore の `var/moneyforward/` に保存し失効時 refresh 自動更新) | **実装済・会計/経費とも実 API e2e 確認済(2026-06-14。login/再利用/refresh)** | [core/oauth.py](core/oauth.py), [core/token_store.py](core/token_store.py), [scripts/oauth_listener.py](scripts/oauth_listener.py), [scripts/cli.py](scripts/cli.py) | REQ-EX01, REQ-EX06 |
| FEAT-AC07 | 検証ゲート(為替・税区分・証憑要件)/ ポリシー lint | **一部実装**(`core/gate.py` + `check_compliance.py --drafts`=下書きの error 級点検。pre-commit/CI 連携・適用開始日版選択は残) | [core/gate.py](core/gate.py), [scripts/check_compliance.py](scripts/check_compliance.py) | REQ-EX02, REQ-TX01 |
| FEAT-AC08 | エージェント本体(取り込み・抽出・登録・会計連携・仕訳調整) | **一部実装**(取込〜抽出〜検証〜下書き=FEAT-AC12。実登録・会計連携は将来) | [core/](core/) | REQ-EX01〜04 |
| FEAT-AC09 | docs の SharePoint ミラー(git main → `Accounting/Docs/`・ADR-0010) | 実装済 | [sharepoint.config.json](sharepoint.config.json) + shared/hooks | REQ-AC03 |
| FEAT-AC12 | 経費レシート取込パイプライン(`ac expense`: refdata/pull/process/push/status/drafts/csv。SharePoint master・Claude 抽出のサイドカー継ぎ目・日付_支払先リネーム・画像トリミング(原本保持)・重複排除(承認上書き・版履歴)・前期実績ベースの費目/税区分・下書き+CSV 生成。会計勘定科目は MF 自動変換) | **実装済**(2026-06-14。ネットワーク非依存テスト緑。実 API 疎通=refdata 確認済) | [scripts/expense_pipeline.py](scripts/expense_pipeline.py), [core/ingest.py](core/ingest.py), [core/refdata.py](core/refdata.py), [core/policy.py](core/policy.py), [core/gate.py](core/gate.py), [core/register.py](core/register.py), [scripts/mf_expense_api.py](scripts/mf_expense_api.py) | REQ-EX01〜04 |
| FEAT-AC13 | クラウド経費への API 登録(`ac expense register`)+ inbox 整理(`clean-inbox`)。`POST me/ex_transactions` に `receipt_input`(証憑画像)を同梱し **1コールで登録+証憑添付=電帳法対応**(CSV 取込は証憑不可)。費目/税区分は前期実績の名前→ID で解決。摘要は店名先頭・メモに為替レート適用。`PUT`(`update_ex_transaction`)で補正可。既定ドライラン・`--confirm` で本番送信/削除。登録済みのみ inbox 削除 | **実装済・実機登録確認済**(2026-06-15。明細番号946・証憑添付=true) | [core/register.py](core/register.py), [scripts/mf_expense_api.py](scripts/mf_expense_api.py), [scripts/expense_pipeline.py](scripts/expense_pipeline.py) | REQ-EX01, REQ-EX03 |
| FEAT-AC14 | 経費明細台帳の Excel 出力(`ac expense xlsx [--push]`)。証憑サムネイル埋込 + クラウド経費明細番号 + サイドカー相当の内容を 1行=1明細で一覧化し、ドキュメント/Expense/ 直下へ。将来はクラウド経費の過去分も同形式で取込 | **実装済**(2026-06-15。実機確認済) | [scripts/expense_xlsx.py](scripts/expense_xlsx.py), [scripts/expense_pipeline.py](scripts/expense_pipeline.py) | REQ-EX03, REQ-AC03 |
| FEAT-AC15 | Teams 通知(`scripts/notify.py` + `ac expense notify`)。クラウド経費へ登録時に OPERATIONS チャネルへ明細詳細を Adaptive Card で送信(Power Automate Workflows)。env=`TEAMS_AC_WORKFLOW_URL[_<CHANNEL>]`・best-effort | **実装済・実機送信確認済**(2026-06-15。明細946) | [scripts/notify.py](scripts/notify.py), [scripts/expense_pipeline.py](scripts/expense_pipeline.py) | REQ-EX02 |
| FEAT-AC16 | 複数レシート PDF の分割(`ac expense split`)。1ファイルに複数レシートが入った PDF を、Claude が書く**分割サイドカー**(`split/<name>.json`: `parts:[{pages,suffix}]` / `mode:per_page`)に従い **1ファイル1レシート**へ分割(`pypdf`)。**計画・検証は純粋な `core/pdfsplit`**(ページ1始まり・範囲外/空/suffix 重複を実行前に弾く=pypdf の不透明な IndexError を回避)、**実 pypdf 操作は `scripts/pdfproc`**。既定ドライラン・`--confirm` で実分割。元 PDF は削除せず `var/expense/split_src/` へ退避(復元可)。分割サイドカーが無いファイルは分割しない(複数ページ≠複数レシート) | **実装済**(2026-06-15。実 pypdf でページ分割を検証) | [core/pdfsplit.py](core/pdfsplit.py), [scripts/pdfproc.py](scripts/pdfproc.py), [scripts/expense_pipeline.py](scripts/expense_pipeline.py) | REQ-EX01 |
| FEAT-AC17 | 過去分の確認・補正(`ac expense import-past` / `revise-past`)。MF 内蔵 OCR の精度が低いため、**今期(未締め)**の既存クラウド経費明細を取込み、証憑画像(`GET .../{id}/mf_file`)をDL → Claude が再読込 → **当期ポリシーを再適用** → MF 現値との差分を **変更フィールドのみ** `PUT` で補正。**差分/PUTボディは純粋な `core/revise`**(`MFCurrent`・`diff_entry`・`build_update_body`。名前正規化/Decimal 比較・ID 解決)、証憑DL/PUT は `scripts/`。既定ドライラン・`--confirm`・`--id`・`--rewrite-remark`。証憑は再アップロードしない(再OCR回避)。証憑なし明細は WEB 手動フラグ。冪等(`past_<id>`・差分ゼロ skip・補正後スナップショット更新)。台帳(xlsx)に過去分も記録 | **実装済**(2026-06-15。無ネットワークテスト緑) | [core/revise.py](core/revise.py), [scripts/mf_expense_api.py](scripts/mf_expense_api.py), [scripts/expense_pipeline.py](scripts/expense_pipeline.py) | REQ-EX01, REQ-EX02 |
