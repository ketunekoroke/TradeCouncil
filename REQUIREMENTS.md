# 要件一覧(Requirements)

TradeCouncil の要件を ID 付きで棚卸しする管理表。**正式仕様の一次資料は
[docs/01_要件定義書.md](docs/01_要件定義書.md)**(FR-x はそこからの引き継ぎ)。
シナリオ・人格・LLMブリッジ基盤(SC/DL/DR/BR/PT/PE/LB/FI/SP/NF 節)の一次資料は
[docs/07_シナリオ・人格基盤.md](docs/07_シナリオ・人格基盤.md)(prototype から統合 — BL-038。
旧→新 ID 対応は本ファイル末尾)。
実装は [FEATURES.md](FEATURES.md)、検証は [TESTCASES.md](TESTCASES.md) を参照。
機能を追加・変更したら、本ファイル → FEATURES → TESTCASES を併せて更新する。

- 優先度: **必須** / **推奨** / **任意**
- 状態: **実装済**(Phase 0)/ **Phase N**(将来)/ **仕様**(プロトコルとして定義・AI が手動実行)

---

## G. ガバナンス(FR-10 — 本フレームワークの中核)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-G01 | ポリシーレジストリ: 全運用ルールをバージョン管理し、active のみ読み込む(FR-10.1) | 必須 | 実装済 |
| REQ-G02 | 決裁フロー: 提案→審議→決裁(owner のみ)→適用→監査ログ。決裁レコードなしの本番反映経路を持たない(FR-10.2) | 必須 | 実装済 |
| REQ-G03 | 同期意思決定会議: ペルソナ審議 + 利用者の決裁宣言で決裁レコード+ポリシーが生成される(FR-10.3) | 必須 | 仕様(scenarios/council.md) |
| REQ-G04 | 委任ルール: P-01 で定義した範囲のみ decision_gate が自動適用(初期値: 委任なし)。P-01 自体の変更は常に決裁事項(FR-10.4) | 必須 | 実装済 |
| REQ-G05 | 不変条項: 決裁権・LLM非執行・監査ログ・キルスイッチ・fail-closed は議題にできない(FR-10.5) | 必須 | 実装済(gate reject + hooks) |
| REQ-G06 | 見直しサイクル: review_after 到来で会議へ再上程(FR-10.6) | 推奨 | 一部(policy list で確認。自動上程は Phase 4) |
| REQ-G07 | 決裁履歴は append-only(ロールバック = 旧値の再決裁) | 必須 | 実装済 |

## R. リスク管理(FR-5)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-R01 | fail-closed: ★P-01〜P-04 が active でない領域では発注拒否。ポリシーキー欠落でも拒否 | 必須 | 実装済 |
| REQ-R02 | 全注文が risk_guard を通る(BOT→取引所の直接経路なし。型レベルで遮断) | 必須 | 実装済 |
| REQ-R03 | 1取引/日次/週次の損失上限、総エクスポージャー、ポジション数上限(FR-5.1〜5.4) | 必須 | 実装済 |
| REQ-R04 | サーキットブレーカ: 価格急変・スプレッド異常・データ鮮度切れ(FR-5.5) | 必須 | 実装済 |
| REQ-R05 | キルスイッチ: コマンド/フラグファイルから全BOT即時停止。解除は人間専用(FR-5.6) | 必須 | 実装済 |
| REQ-R06 | 資産クラス別レバレッジ上限 + 口座実効レバレッジ上限(FR-5.8) | 必須 | 実装済 |
| REQ-R07 | 証拠金監視・オーバーナイト規則(FR-5.9〜5.10) | 必須 | Phase 6(信用・先物導入時) |
| REQ-R08 | リスクモジュールの単体テスト カバレッジ90%以上 | 必須 | 実装済(92%) |

## E. 執行・記録(FR-4 / FR-6)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-E01 | すべての発注に decision_id(根拠へ遡及可能。FR-4.4) | 必須 | 実装済 |
| REQ-E02 | 注文の冪等性(二重発注防止)、ペーパーモード(FR-4.2) | 必須 | 実装済 |
| REQ-E03 | 再起動時のポジション復元・取引所との突合(FR-4.2) | 必須 | 実装済(reconcile) |
| REQ-E04 | 全注文・約定・拒否・インシデントを根拠付きでDB保存(FR-6.1) | 必須 | 実装済 |
| REQ-E05 | KPI集計と根拠連鎖の機械検証 | 必須 | 実装済(最小版) |
| REQ-E06 | 週次レビュー自動化・悪BOT状態遷移(FR-6.2〜6.5) | 必須 | Phase 4 |

## M. マルチアセット・ホライズン(FR-8 / FR-9)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-M01 | 統一インストゥルメントモデル(資産クラス非依存IF。FR-8.1) | 必須 | 実装済 |
| REQ-M02 | ブローカーアダプタ共通IF(FR-8.2)。実装は paper(模擬約定)+ bybit_testnet(実接続・模擬資金 — ADR-0008。mainnet 発注経路なし) | 必須 | 実装済 |
| REQ-M06 | JPY 以外の instrument 通貨は保守的固定レート(fx.usdjpy_rate ≥ 1)で JPY 換算しリスクチェックする。未設定・未対応通貨は起動拒否(fail-closed)。DB の一次記録は instrument 通貨建てを維持(ADR-0008) | 必須 | 実装済 |
| REQ-M03 | セッションカレンダー(FR-8.3)。未知カレンダーは閉場扱い | 必須 | 最小実装(24/7のみ) |
| REQ-M04 | マルチ通貨・円建て統合評価(FR-8.4) | 必須 | Phase 6 |
| REQ-M05 | バー駆動/スケジュール駆動の2系統・スリーブ配分(FR-9) | 必須 | バー駆動のみ実装済 |

## D. データ収集・ニュース(FR-1 / FR-2)/ 会議自動化(FR-3)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-D01 | 市場データ収集(WS・実勢価格) | 必須 | 一部実装済(Bybit REST kline/ticker — ADR-0008。実データ取得を実測確認済。WS・market_collector は Phase 1) |
| REQ-D02 | ニュース収集・3段フィルタ・イベントシグナル | 必須 | Phase 2 |
| REQ-D03 | 戦略会議の API 自動実行(council_runner・決議JSON) | 必須 | Phase 3 |

## O. 監視・運用(FR-7)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-O01 | Teams/Discord 通知(backend 切替・Teams は Adaptive Card で構造化可視化・専用 Team の複数チャネルへ severity/用途別ルーティング・URL 未設定時はフォールバック — ADR-0002/0003) | 必須 | 実装済 |
| REQ-O02 | heartbeat 死活監視 + 途絶検知 | 必須 | 実装済(自動再起動は Phase 1/VPS) |
| REQ-O03 | LLM APIコストメーター(FR-7.3) | 必須 | Phase 2(テーブル定義のみ) |
| REQ-O04 | TC_VAR_DIR で実行時生成物(DB・KILL・ログ)一式を別ディレクトリへ分離できる(サンドボックス)。未設定時は完全に従来挙動(ADR-0004) | 必須 | 実装済 |
| REQ-O05 | 開発機から本番データを観測する経路をデータ種別で確定(git 一方向・本番 push なし・SSH ライブ読取・tc snapshot・SharePoint・Notion ミラー — ADR-0005)。生 WAL DB は直接コピーしない | 必須 | 実装済(snapshot)/ Phase 1(SSH 経路) |
| REQ-O06 | 中央集権的な構造化(JSON)ログを config で切替(CloudWatch Logs 取り込み用)。既定 plain で後方互換(ADR-0006) | 必須 | 実装済 |

## SC. シナリオ・アーキテクチャ(MAGI 継承)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-SC01 | ルーター(CLAUDE.md)がモード判定・シナリオ選択を行う | 必須 | 仕様 |
| REQ-SC02 | 各シナリオは独立した1ファイル(scenarios/<name>.md)。共通作法は CLAUDE.md を唯一の出典とする | 必須 | 仕様 |
| REQ-SC03 | 人格ごとに LLM バックエンド(claude/openai/gemini)を frontmatter で切替できる | 必須 | 実装済(ブリッジ) |
| REQ-SC04 | MAGI 4シナリオ(合議/資料レビュー/ブレスト/人格テスト)がルートで実行できる | 推奨 | 仕様(コピー済) |
| REQ-SC05 | シナリオごとに出力先を分離(workspace/council 等)。root は単一の workspace/ で、SharePoint 連携時は全フォルダが双方向自動同期される(追加型・newer-wins・削除非伝播 — ADR-0009) | 推奨 | 実装済 |
| REQ-SC06 | council シナリオ: 式次第(docs/03 第3章)準拠、決裁適用は tc policy record のみ、veto は審議差し戻し | 必須 | 仕様 |
| REQ-SC07 | 成果物: 議論ログ等を Markdown で常に生成する。Excel(マトリクス)/ Word(レポート)/ チャートは任意生成 | 必須/任意 | 仕様 |
| REQ-SC08 | どの人格がどの backend/model で動いたかを各成果物の冒頭に明記する(再現性。フォールバック発火時は実際のモデル) | 推奨 | 仕様 |

## DL. 合議シナリオ(deliberation — 一次: docs/07・scenarios/deliberation.md)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-DL01 | 価値観で分かれた3人格(MELCHIOR=論理 / BALTHASAR=関係性 / CASPER=直感)が議論する | 必須 | 仕様 |
| REQ-DL02 | ファシリテーター(メインセッション)が論題整理・進行・合意形成・成果物生成を担う | 必須 | 仕様 |
| REQ-DL03 | 議題の重要度に応じ Lite / Standard / Full の3モードを選べる | 必須 | 仕様 |
| REQ-DL04 | Round 0〜9 のプロトコルに沿って進行する(モードごとに含むラウンドが変わる) | 必須 | 仕様 |
| REQ-DL05 | 投票は確信度を加味し、反対票・少数意見を最終回答に必ず残す | 必須 | 仕様 |
| REQ-DL06 | 各人格は「意図的な弱み」を保持し、予定調和に丸めない | 必須 | 仕様 |
| REQ-DL07 | claude 人格間は SendMessage で直接対話できる(使えない環境ではファシリテーター仲介=ダイジェスト/`--history` にフォールバック) | 推奨 | 仕様 |

## DR. 資料チェック&リバイス シナリオ(document-review — 一次: docs/07・scenarios/document-review.md)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-DR01 | 3人格の価値観レンズをレビュー観点に投影する(MELCHIOR=正確性/論理 / BALTHASAR=読者/伝わりやすさ / CASPER=訴求力/声)。専門の編集者・校正者には作り変えない | 必須 | 仕様 |
| REQ-DR02 | Round 0 で資料の素性(種類/目的/対象読者/トーン)と改訂の制約(不可変箇所・分量・リライト強度)を確定する | 必須 | 仕様 |
| REQ-DR03 | 各人格が同じ資料を独立にレビューし、重要度・箇所・問題・修正提案つきで指摘する | 必須 | 仕様 |
| REQ-DR04 | 深度モード Quick / Standard / Deep を選べる(相互レビュー・優先度合意・再検証の有無が変わる) | 必須 | 仕様 |
| REQ-DR05 | 衝突する指摘をファシリテーターが裁定し、must/should/nice/見送りに分類する | 必須 | 仕様 |
| REQ-DR06 | 合意した指摘を反映した改訂版を**元と同形式**で生成し、変更履歴を記録する | 必須 | 仕様 |
| REQ-DR07 | 成果物として指摘レポートと改訂版の両方を出す(指摘だけ・改訂だけにしない) | 必須 | 仕様 |
| REQ-DR08 | 未採用の指摘とその理由を成果物に残す(少数意見の保持に相当) | 推奨 | 仕様 |
| REQ-DR09 | 原文を尊重し、指摘を捏造せず、合意範囲を超えた全面リライトをしない | 必須 | 仕様 |
| REQ-DR10 | docx 等書式付き資料の改訂版は原則「原本コピー編集」で体裁・画像を保ち、全面再構築は構造変更時の最終手段とする(失う体裁を Round 0 で確認) | 推奨 | 仕様 |

## BR. ブレストシナリオ(brainstorm — 一次: docs/07・scenarios/brainstorm.md)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-BR01 | 3人格の価値観レンズを**アイデアの発散と評価の両方**に投影する(MELCHIOR=実現可能/体系 / BALTHASAR=人中心/持続 / CASPER=大胆/独創)。専門の企画者・コンサルには作り変えない | 必須 | 仕様 |
| REQ-BR02 | Round 0 で目的・制約・成功基準・評価軸(3〜6)と発散ルール(判断後回し/量優先/突飛歓迎/build-on)を確定する | 必須 | 仕様 |
| REQ-BR03 | モード Quick / Standard / Deep を選べる(発散巡数・プレモーテムの有無が変わる)。発散巡数はモード固定で、新案が尽きたら早期収束する | 必須 | 仕様 |
| REQ-BR04 | 各人格が独立に大量発散し(質より量)、アイデアマップ化(クラスタ化・白地の明示)してから build-on・掛け合わせ・空白埋めで二次発散する | 必須 | 仕様 |
| REQ-BR05 | ショートリストの各アイデアを全人格が評価軸で 0〜10 採点し一言根拠を付す | 必須 | 仕様 |
| REQ-BR06 | スコア集計はレンズ別内訳を保持し(単純平均で潰さない)、上位 N に加えて評価が割れた尖り案を別枠で残す | 必須 | 仕様 |
| REQ-BR07 | 上位案を3レンズ協働でブラッシュアップ(CASPER訴求×MELCHIOR実現×BALTHASAR人/持続)。Deep ではプレモーテムで事前ストレステスト | 必須 | 仕様 |
| REQ-BR08 | 成果物としてアイデア集・アイデアマップ(Mermaid)・評価マトリクス・上位案を出す。アイデア・スコアを捏造しない | 必須 | 仕様 |
| REQ-BR09 | 発散と収束を分離する(発散フェーズでは批判させない)。意図的な弱みを保持し、レンズの差を発散にも評価にも残す | 推奨 | 仕様 |

## PT. 人格テストシナリオ(persona-test — 一次: docs/07・scenarios/persona-test.md)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-PT01 | 同一のプローブ(依頼)を全人格に等しく独立に投げ、互いの出力を見せずに答えさせる(議論させない=個性を分離) | 必須 | 仕様 |
| REQ-PT02 | 固定のプローブ・バッテリ(判断 / 好奇心 / 弱み / ドメイン非依存 / 反応)を持ち、回帰テストとして再現可能にする(ユーザーは追加可、既定は不変) | 必須 | 仕様 |
| REQ-PT03 | 各人格の定義(レンズ / 好奇心の向き / 意図的な弱み / 声)をルーブリックに、期待と照合して人格別に判定する。弱みが出るのは仕様どおりと評価する | 必須 | 仕様 |
| REQ-PT04 | 識別性チェック(2人格が似すぎ=個性の潰れ・混ざりを検出して警告する) | 必須 | 仕様 |
| REQ-PT05 | backend は既定で各人格の設定どおり(混在可)、任意で同一 backend に揃えられる(混在時は出力差にモデル差が混じりうると注記) | 推奨 | 仕様 |
| REQ-PT06 | モード Quick / Standard / Deep を選べる(プローブ範囲・複数 backend・ベースライン回帰の有無が変わる) | 推奨 | 仕様 |
| REQ-PT07 | (任意)過去の実行をベースラインに差分比較し、調整で狙った変化が出たか・他の特性が壊れていないかを確認する | 任意 | 仕様 |
| REQ-PT08 | 成果物として使用した設問・差分マトリクス・人格別判定を出す。出力を期待に寄せて歪めず、中身・判定を捏造しない | 必須 | 仕様 |

## PE. 人格定義(一次: docs/07・docs/02 §5)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-PE01 | 人格は専門分野ではなく価値観・判断軸で定義する(専門ペルソナにしない。TradeCouncil 5ペルソナは「視点の偏り」を同思想で設計 — docs/02 §5) | 必須 | 実装済 |
| REQ-PE02 | 人格は frontmatter に `name` / `description` / `backend` / `model` を持つ | 必須 | 実装済 |
| REQ-PE03 | 人格定義の本文がそのままシステムプロンプトになる(フロントマター・メタコメントは除外) | 必須 | 実装済 |
| REQ-PE04 | 3人格は共通の「好奇心・興味」を持ち、向く対象と強度がレンズごとに屈折する(向きにくい対象は「意図的な弱み」と呼応。弱みは削らない) | 推奨 | 実装済 |

## LB. LLM バックエンド選択(一次: docs/07)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-LB01 | 各人格ごとに Claude / ChatGPT(OpenAI)/ Gemini を frontmatter(`backend`/`model`)で選べる | 必須 | 実装済 |
| REQ-LB02 | `backend: claude` は通常召喚、`openai`/`gemini` は各ブリッジ経由で動かす | 必須 | 実装済 |
| REQ-LB03 | claude / openai / gemini 人格は混在でき、議論プロトコル(ラウンド/投票/採点)は不変 | 必須 | 仕様 |
| REQ-LB04 | openai/gemini 人格はステートレスなので、必要文脈を毎ラウンド渡して継続性を保つ(stdin/`--input` への織り込み、または `--history` での多ターン会話履歴) | 必須 | 実装済 |
| REQ-LB05 | openai/gemini 人格の拒否/空応答時は自動で再試行する(既定1回、`MAGI_GEN_MAX_RETRIES`)。続く場合はファシリテーターが人格定義を変えず指示を言い換えて再実行 | 推奨 | 実装済 |
| REQ-LB06 | プロバイダ間で共通する処理(キー解決/抽出/HTTP等)は共有モジュール(scripts/bridge_common.py)に集約する | 推奨 | 実装済 |
| REQ-LB07 | 過負荷/モデル不在(429/5xx/404・model_not_found)で primary が失敗した場合、指定があれば代替モデルへ1回フォールバックできる(実際に使ったモデルを成果物に明記) | 推奨 | 実装済 |

## FI. ファイル入力(一次: docs/07)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-FI01 | 画像(jpg/png/gif/webp)を全 backend でネイティブに扱える | 必須 | 実装済 |
| REQ-FI02 | PDF を openai/gemini 人格でネイティブに扱える(テキストを持つ PDF) | 必須 | 実装済 |
| REQ-FI03 | Office(docx/xlsx/pptx)を準ネイティブに扱える(ローカルでテキスト抽出。docx は段落と表をドキュメント順に保つ) | 推奨 | 実装済 |
| REQ-FI04 | テキストファイル(txt/md/csv/json 等)を本文として注入できる | 推奨 | 実装済 |
| REQ-FI05 | 同一ファイルを全人格に等しく渡せる(claude/openai/gemini で挙動が対称) | 必須 | 実装済 |
| REQ-FI06 | 同じ画像/PDF を多ラウンドで使う場合、アップロードして file_id を使い回せる | 推奨 | 実装済 |

## SP. SharePoint 連携(任意 — 一次: docs/07・ADR-0009)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-SP01 | `workspace/` を SharePoint ドキュメントライブラリと同期できる(scripts/sharepoint.py / Microsoft Graph) | 任意 | 実装済 |
| REQ-SP02 | `enabled` は**同期通信の有無のみ**を制御する(作業場所は常に workspace/ — ADR-0009)。設定は env(.env)→ sharepoint.config.json の順で解決 | 任意 | 実装済 |
| REQ-SP03 | `sync` は双方向・追加型・更新時刻の新しい方優先・**削除非伝播**・mtime 整合(再実行で skip)。folders は council 含む全シナリオ出力 | 任意 | 実装済 |
| REQ-SP04 | 認証はアプリ(クライアントシークレット)。`MAGI_SHAREPOINT_*` を .env で解決しコミットしない | 任意 | 実装済 |
| REQ-SP05 | `sync` を主とし、`pull`/`push` は選択的リカバリ用に存続。`info` で SharePoint URL を提示できる | 任意 | 実装済 |
| REQ-SP06 | SharePoint 連携も標準ライブラリだけで動く(HTTP は bridge_common 経由、新規 pip 依存なし) | 推奨 | 実装済 |
| REQ-SP07 | Azure アプリ登録のセットアップ手順を docs/setup/ にマニュアル化し、記載内容(env 名・必要権限・コマンド)をコード/設定と一致させる | 推奨 | 実装済 |
| REQ-SP08 | セットアップ手順は外部仕様(Entra ID 画面・Microsoft Graph)に依存するため、定期的に最新の公式情報を確認し、変更があればマニュアルを更新する | 任意 | 仕様 |

## NF. ブリッジ非機能(一次: docs/07)

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-NF01 | ブリッジは標準ライブラリで動く(Office 抽出時のみ追加ライブラリ) | 推奨 | 実装済 |
| REQ-NF02 | クロス OS(Windows/macOS/Linux)で動作し、日本語を UTF-8 で入出力する(stdin 入力・stdout 出力とも) | 必須 | 実装済 |
| REQ-NF03 | エラーはトレースバックでなく整形メッセージで返す | 推奨 | 実装済 |
| REQ-NF04 | トークン消費が通常の数倍になる点をドキュメントで明示する | 任意 | 実装済 |
| REQ-NF05 | 一過性の HTTP エラー(429 / 5xx / 接続タイムアウト)は指数バックオフ + ジッタで自動リトライする(`Retry-After` 尊重、回数・タイムアウトは `MAGI_HTTP_*` で調整可) | 推奨 | 実装済 |

## N. ナレッジ・戦略開発

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-N01 | 戦略カタログ: 戦略のノウハウ(仮説・パラメータ根拠・検証の解釈・運用の学び)を docs/strategies/ に1戦略1カードで蓄積する。学びは append-only、実績数値は DB が真実源(ADR-0007) | 必須 | 実装済(運用は仕様) |
| REQ-N02 | BOT スキャフォールド: `tc bot new` で戦略雛形4ファイル(bots/・config/bots/・tests/bots/・戦略カード)を一括生成。既存ファイルがあれば何も書かず拒否、config は enabled: false 既定、レジストリ登録は自動編集せずテスト駆動で誘導 | 必須 | 実装済 |

## S. 安全・開発体制

| ID | 要件 | 優先度 | 状態 |
|---|---|---|---|
| REQ-S01 | 秘密情報をコード・コミットに含めない(hooks + pre-commit で検査) | 必須 | 実装済 |
| REQ-S02 | 実弾系・キル解除コマンドを AI からブロック(hooks) | 必須 | 実装済 |
| REQ-S03 | config/generated・config/policies・prototype/ の手編集をブロック | 必須 | 実装済 |
| REQ-S04 | core/risk・governance の差分は risk-auditor で審査 | 必須 | 仕様(サブエージェント) |
| REQ-S05 | たたき台数値をコード・テストにハードコードしない | 必須 | 実装済(テストは明白なテスト値) |
| REQ-S06 | ブリッジ類のシークレットは 環境変数 → ルート `.env` → `.claude/settings.local.json` の3段で解決し、placeholder/空は未設定扱い。テンプレート(`.env.example`)のみ git 追跡する | 必須 | 実装済 |

---

## 付録: prototype からの ID 対応表(BL-038 統合・トレーサビリティ)

prototype/REQUIREMENTS.md の旧 ID → 本ファイルの新 ID(衝突回避のための再割当て):

| 旧(prototype) | 新(本ファイル) | 備考 |
|---|---|---|
| REQ-SC01〜06 | REQ-SC01〜06 に吸収 | 既存 SC 節と同趣旨のため統合 |
| REQ-O01/O02(成果物) | REQ-SC07 | シナリオ共通要件として吸収 |
| REQ-SC06・REQ-B04(モデル明記) | REQ-SC08 | 重複統合 |
| REQ-D01〜D07(合議) | REQ-DL01〜DL07 | ルート D(データ収集)と衝突 |
| REQ-R01〜R10(資料レビュー) | REQ-DR01〜DR10 | ルート R(リスク)と衝突 |
| REQ-BR01〜BR09 / REQ-PT01〜PT08 | 同番のまま | 衝突なし |
| REQ-P01〜P04(人格) | REQ-PE01〜PE04 | |
| REQ-B01〜B03, B05〜B08 | REQ-LB01〜LB07 | B04 は SC08 へ |
| REQ-F01〜F06(ファイル入力) | REQ-FI01〜FI06 | |
| REQ-S01〜S03(シークレット) | REQ-S06 に統合 | 現行 .env 集約仕様で1行化(検査は REQ-S01) |
| REQ-SP01〜SP08 | REQ-SP01〜SP08(**内容は ADR-0009 仕様に全面改訂**) | 旧 local/sharepoint 切替仕様は廃止 |
| REQ-N01〜N05(非機能) | REQ-NF01〜NF05 | ルート N(ナレッジ)と衝突 |
